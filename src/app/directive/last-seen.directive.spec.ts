import { LastSeenDirective } from './last-seen.directive';

describe('LastSeenDirective', () => {
  it('should create an instance', () => {
    const directive = new LastSeenDirective();
    expect(directive).toBeTruthy();
  });
});
